# About
Cache friendly list on arrays

# TODO
[\*] default node is scary                                   \
[\*] в DUMP проходимся только по массиву, а не по next/prev  \
[\*] bidirectional nodes                                     \
[\*] непарные стрелки жирные                                 \
[ ] (уже ненужно) В верификаторе ходим по связям, но только size раз. Если меньше size раз, то значит не хватает элементов \
[ ] canary                                                  \                                               
[ ] Переменное количество аргументов в дампе                \
[ ] Прочитать про attirubute для printf чтобы получать все ошибки форматной строки \
# OPTIONAL: 
[\*] Почитать про rank и сделать head и free